@if (!string.IsNullOrEmpty(Message))
{
    <div class="sr-only" aria-live="@AriaLive" aria-atomic="true" role="status">
        @Message
    </div>
}

@code {
    [Parameter] public string? Message { get; set; }
    [Parameter] public string AriaLive { get; set; } = "polite";
}
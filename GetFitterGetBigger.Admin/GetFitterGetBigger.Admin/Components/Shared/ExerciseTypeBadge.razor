@using GetFitterGetBigger.Admin.Models.Dtos

<span class="inline-flex items-center px-2.5 py-0.5 rounded-md text-xs font-medium @GetTypeColorClass()">
    <span class="mr-1">@GetTypeIcon()</span>
    @TypeName
</span>

@code {
    [Parameter, EditorRequired] public string TypeName { get; set; } = string.Empty;

    private string GetTypeColorClass()
    {
        return TypeName switch
        {
            "Warmup" => "bg-amber-100 text-amber-800 border border-amber-200",
            "Workout" => "bg-blue-100 text-blue-800 border border-blue-200",
            "Cooldown" => "bg-emerald-100 text-emerald-800 border border-emerald-200",
            "Rest" => "bg-purple-100 text-purple-800 border border-purple-200",
            _ => "bg-gray-100 text-gray-800 border border-gray-200"
        };
    }

    private string GetTypeIcon()
    {
        return TypeName switch
        {
            "Warmup" => "🔥",    // Fire emoji for warmup
            "Workout" => "💪",   // Flexed bicep for workout
            "Cooldown" => "❄️",  // Snowflake for cooldown
            "Rest" => "⏸️",      // Pause for rest
            _ => "📝"           // Default memo
        };
    }
}